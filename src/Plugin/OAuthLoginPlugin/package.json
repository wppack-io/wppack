{
  "name": "@wppack/oauth-login-plugin",
  "version": "1.0.0",
  "private": true,
  "author": "WpPack",
  "scripts": {
    "build": "wp-scripts build",
    "start": "wp-scripts start",
    "make-pot": "makepot . languages --domain=plugin --slug=wppack-oauth-login --headers=email:dev@wppack.io",
    "make-json": "makejson js/build languages --slug=wppack-oauth-login"
  },
  "devDependencies": {
    "@wordpress/scripts": "^30.0.0",
    "@wp-blocks/make-pot": "^1.6.0"
  }
}
